# Dummy values so the test suite can boot without real infrastructure.
# Committed on purpose — tests must run on a clean clone. Never put real
# credentials here; those belong in .env, which is gitignored.
NODE_ENV=test
DATABASE_URL=postgresql://wb:wb@localhost:5432/wbnote_test
REDIS_URL=redis://localhost:6379
KAFKA_BROKERS=localhost:29092
